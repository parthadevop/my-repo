node('linux') { // Use a node with the label 'linux'
stage('Server Details') { // Define a stage called 'Server Details'
sh 'bash server_details.sh' // Run a bash script that prints server details
}
}
