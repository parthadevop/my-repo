pipeline {
    agent { label 'Node1' }

    stages {
        stage('Echo Hostname') {
            steps {
                echo "Hostname: ${env.HOSTNAME}"
            }
        }
    }

    post {
        failure {
            node('NODE2') {
                echo "Hostname: ${env.HOSTNAME}"
            }
        }
    }
}
